# Upstream Sync — 2026-07-27

Generated by automated weekly sync routine.

## Status

This branch is a **tracking placeholder** — the actual upstream commits cannot be applied automatically because 18 [BREAKING] changes require manual reconciliation. See the PR description for instructions.

## Upstream

- Repo: https://github.com/qwibitai/nanoclaw
- Target HEAD: `879835bf8bb882672742f0765747312c4948420f`
- Commits ahead of fork baseline: **2004**
- Fork baseline (merge-base): `298c3eade4a8497264844aa29e71bee7dadf3a89`

## To apply manually

```bash
git remote add upstream https://github.com/qwibitai/nanoclaw.git 2>/dev/null || true
git fetch upstream main
git checkout main
git merge upstream/main   # resolve conflicts, then:
pnpm install
pnpm run build
pnpm exec tsx scripts/upgrade-state.ts set
git push origin main
```

## Breaking changes (18)

1. **Provider-agnostic memory** — run `/migrate-memory` on each group before restart.
2. **Channel install skills are sole source of truth** — `setup/add-<channel>.sh` deleted; use `/add-<channel>`.
3. **whatsapp-formatting / slack-formatting moved to channels branch** — re-run `/add-whatsapp` / `/add-slack`.
4. **Explicit `to` destination required in send_message/send_file** — update custom task instructions; rebuild agent image.
5. **Scheduled tasks → `ncl tasks`** — MCP scheduling tools removed; migrate per docs/ncl-tasks-migration.md.
6. **Chat SDK pinned to 4.29.0** — re-run `/add-<channel>` for every installed channel adapter.
7. **@onecli-sh/sdk 0.5.0 → 2.2.1** — requires OneCLI server with /v1 API; see docs/onecli-upgrades.md.
8. **Startup requires upgrade marker** — before restart: `pnpm exec tsx scripts/upgrade-state.ts set`.
9. **Service names now per-install** (slugged to project root) — update launchctl/systemctl commands.
10. **New entity model** — users, roles, messaging groups, and agent groups are now separate entities.
11. **Two-DB session split** — inbound.db + outbound.db replace single shared session DB.
12. **Install flow replaced** — `bash nanoclaw.sh` is the new default.
13. **Channels moved to channels branch** — trunk no longer ships Discord, Slack, Telegram, WhatsApp, etc.
14. **Alternative providers moved to providers branch** — Codex, OpenCode, Ollama install via skills.
15. **Three-level channel isolation** — new session_mode per channel; configure via `/manage-channels`.
16. **Apple Container removed from default setup** — opt in via `/convert-to-apple-container`.
17. **Pino logger replaced** — WhatsApp users must re-merge the WhatsApp fork.
18. **OneCLI Agent Vault replaces built-in credential proxy** — Docker: run `/init-onecli`; Apple Container: re-merge skill branch.

## Custom files to reconcile after merge

These are fork-specific and not in the upstream; check for conflicts after `git merge`:
- `container/agent-runner/src/mcp-tools/{caldav,clockify,imap,kimai,notion,wave}.ts`
- `src/transcription.ts`
- `groups/main/CLAUDE.local.md`
