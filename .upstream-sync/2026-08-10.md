# Upstream sync — 2026-08-10

Sync date: 2026-08-10  
Fork base (merge-base with upstream): `298c3eade4a8497264844aa29e71bee7dadf3a89`  
Upstream HEAD: `86e353564d2c0f6173fbb4b95f061c822ff6b601`  
Commits ahead of fork: **2059**

This branch starts at `86017ff` (upstream state as of 2026-08-03). The diff vs `main` on this PR covers the full upstream delta from the fork's merge-base through `86017ff`.

This week's additional commits (`86017ff..86e353`, ~20 commits) could not be pushed automatically because git push is blocked in the scheduled runner environment. To incorporate them:

```bash
git fetch upstream main
git checkout automated-update/20260810
git merge upstream/main --no-edit
git push origin automated-update/20260810
```

## This week's new upstream commits (86017ff → 86e353)

```
86e3535 Merge pull request #3216 — docs(hardened-image): package scope note
774ec95 Merge branch 'main' into docs/hardened-image-package-scope
00e8e8f Merge pull request #3222 — feat(permissions): privacy-safe DM logs
1ecb952 feat(permissions): add privacy-safe DM logs
5bfdf9a Merge pull request #3213 — refactor(channels): question render registry
44145e3 Merge pull request #3214 — refactor(host): host lifecycle registry
95089b0 Merge pull request #3212 — refactor(db): module migration registry
f41797e Merge pull request #3211 — docs(skills): SRP guidance
a917528 docs(hardened-image): install_packages covers apt and npm only
31d743f refactor(host): add unified module lifecycle registry
6e7b91e refactor(channels): register question renderers
8ef90de refactor(db): add module migration registry
3352461 docs(skills): define single-responsibility integration rule
743e32d chore(skills): remove stale qodo and Google MCP skills (#3172)
358f1a8 Merge pull request #3187 — fix(agent-runner): disallow built-in SendMessage
81b18a9 fix(agent-runner): disallow built-in SendMessage for agent-to-agent messaging
8d04fde Merge pull request #3154 — fix/task-run-time-context
4f4f626 docs: update task prompt shape
a5adb29 Merge branch 'main' into fix/task-run-time-context
86017ff [branch start — automated-update/20260803]
```

## Outstanding [BREAKING] changes requiring human review (20 total)

These span multiple upstream releases and are all unmerged into this fork's main.

1. **Host lifecycle registry** — modules importing `onShutdown()`/`getShutdownCallbacks()` from `response-registry.ts` must migrate. See `docs/host-lifecycle-migration.md`.

2. **Hardened agent image** — existing installs should switch to the Echo-built image. See `docs/hardened-image.md`.

3. **iMessage unified** — `imessage-cloud` channel + `/add-imessage-cloud` skill **removed**. Re-install via `/add-imessage`. Drop `IMESSAGE_SERVER_URL`/`IMESSAGE_API_KEY`.

4. **Provider-agnostic memory** — run `/migrate-memory` on existing groups before restarting.

5. **Channel install skills** — bespoke `setup/add-<channel>.sh` installers deleted; use `/add-<channel>` instead.

6. **`whatsapp-formatting` / `slack-formatting`** moved to channels branch — re-run `/add-whatsapp` or `/add-slack` after updating.

7. **Scheduled tasks → `ncl tasks`** — MCP-based task tools removed. See `docs/ncl-tasks-migration.md`.

8. **Explicit task delivery** — every `send_message`/`send_file` needs a named `to`. Rebuild agent image; clear/compact sessions.

9. **Chat SDK pinned to 4.29.0** — re-run `/add-<channel>` for any installed channel.

10. **`@onecli-sh/sdk` 0.5.0 → 2.2.1** — requires OneCLI `/v1` API. See `docs/onecli-upgrades.md`.

11. **Startup upgrade marker required** — after update, run `pnpm exec tsx scripts/upgrade-state.ts set` before restarting. See `docs/upgrade-recovery.md`.

12. **Per-install service names** — launchd/systemd units now slug-suffixed. Old `com.nanoclaw` / `nanoclaw.service` are invalid.

13. **New entity model** — users/roles/groups are separate entities; privilege is user-level. See `docs/architecture.md`.

14. **Two-DB session split** — `inbound.db` + `outbound.db` replace shared session DB.

15. **Install flow replaced** — `bash nanoclaw.sh` is the new default; `/setup` remains as alternative.

16. **Channels moved to `channels` branch** — Discord, Slack, Telegram, WhatsApp, iMessage, Teams, Linear, GitHub, WeChat, Matrix, Google Chat, Webex, Resend no longer in trunk.

17. **Providers moved to `providers` branch** — OpenCode, Codex, Ollama install via `/add-*` skills.

18. **Three-level channel isolation** — configure via `/manage-channels`.

19. **Apple Container removed from default setup** — opt-in via `/convert-to-apple-container`.

20. **Pino logger replaced** — WhatsApp users: `git fetch whatsapp main && git merge whatsapp/main`.

## Files NOT to touch (custom fork additions)

- `container/agent-runner/src/mcp-tools/{caldav,clockify,imap,kimai,notion,wave}.ts`
- `src/transcription.ts`
- `groups/main/CLAUDE.local.md`

Verify these are intact after merging.

## Recommended merge path

1. Pull this week's extra commits into the branch (see commands above).
2. Review breaking changes and plan migration steps.
3. Merge via `/update-nanoclaw` (guided) or manually.
4. Run `pnpm exec tsx scripts/upgrade-state.ts set` **before** restarting the service.
